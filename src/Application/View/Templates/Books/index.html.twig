{% extends 'base.html.twig' %}

{% block body %}
    <h1>List of Books</h1>
    <ul>
        {% for book in books %}
            <li>{{ book.title }} {{ book.description }}</li>
        {% endfor %}
    </ul>
{% endblock %}